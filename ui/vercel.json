{
	"framework": null,
	"buildCommand": "npm run build",
	"outputDirectory": "build",
	"rewrites": [
		{ "source": "/api/(.*)", "destination": "https://api.traceway.ai/api/$1" },
		{ "source": "/(.*)", "destination": "/index.html" }
	]
}
